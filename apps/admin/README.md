# NMS Admin App

App independiente para administradores.

## Desarrollo

Desde la raiz del repo:

```bash
npm run dev:admin
```

Directamente desde `apps/admin`:

```bash
npm run dev
```

Ruta base local: `http://127.0.0.1:3001`.
