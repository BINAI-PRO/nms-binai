
export default function Page() {
  return (
    <section className='card p-5 space-y-2'>
      <p className='text-xs font-semibold uppercase tracking-[0.12em] text-[var(--muted)]'>Admin · settings</p>
      <h2 className='text-lg font-bold text-[var(--foreground)]'>Pantalla en construcción</h2>
      <p className='text-sm text-[var(--muted)]'>Estructura lista para conectar datos reales, RLS y UI del piloto.</p>
    </section>
  );
}
